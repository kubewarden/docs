# Configuration file to customize the build process of the website on Netlify.
[build]
publish = "build/site"
command = "make community-remote"
# TODO disable google analytics when it is enabled

#[build.environment]
#  NODE_OPTIONS = "--max_old_space_size=4096"
