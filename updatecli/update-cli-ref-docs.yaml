policies:
  - name: Copy CLI reference documentation files
    config:
      - updatecli.d/copy-cli-ref-docs.yaml
  - name: Add header to the CLI reference documentation
    config:
      - updatecli.d/headers.yaml
    values:
      - values.d/headers.yaml
